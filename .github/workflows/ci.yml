name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  quality:
    name: ${{ matrix.os }} quality gate
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]

    steps:
      # actions/checkout v4.4.0 — https://github.com/actions/checkout/releases/tag/v4.4.0
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - name: Install Rust 1.94.1
        # dtolnay/rust-toolchain 1.94.1 — https://github.com/dtolnay/rust-toolchain/commit/75be91dd2711b583df57c31d0873b4145c89f1d9
        uses: dtolnay/rust-toolchain@75be91dd2711b583df57c31d0873b4145c89f1d9
        with:
          components: rustfmt, clippy
      - name: Verify locked metadata
        run: cargo metadata --locked --no-deps --format-version 1
      - name: Check formatting
        run: cargo fmt --all -- --check
      - name: Run Clippy
        run: cargo clippy --workspace --all-targets --locked -- -D warnings
      - name: Run tests
        run: cargo test --workspace --all-targets --locked
      - name: Verify dependency exception expiry and graph
        run: cargo test --locked -p aihack --test dependency_exception_gate
      - name: Verify duplicate dependency budget
        run: cargo test --locked -p aihack --test dependency_duplicate_gate
      - name: Verify canonical R7 and R8 checkpoints
        shell: bash
        run: ./scripts/r7_checkpoint.sh && ./scripts/r8_checkpoint.sh
      - name: Build release artifacts
        run: cargo build --workspace --release --locked
      - name: Verify Linux release bundle
        if: runner.os == 'Linux'
        run: ./build.sh --release
      - name: Verify Windows release bundle
        if: runner.os == 'Windows'
        run: cmd /c build.bat --release
      - name: Install cargo-audit
        run: cargo install --locked cargo-audit --version 0.22.1
      - name: Audit RustSec advisories
        run: cargo audit
      - name: Install cargo-deny
        run: cargo install --locked cargo-deny --version 0.19.4
      - name: Check licenses, bans, and sources
        run: cargo deny check licenses bans sources
      - name: Ensure the lockfile stays unchanged
        run: git diff --exit-code -- Cargo.lock
