[package]
name = "aihack-ai-contract"
version = "0.3.0"
edition = "2021"
rust-version = "1.94"
license = "NGPL"
publish = false

[dependencies]
aihack-core = { path = "../aihack-core", version = "0.3.0" }
serde = { version = "1", features = ["derive"] }
